spring:
  jpa:
    defer-datasource-initialization: true
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:3333
      username: root
      password: password
  bootstrap:
    enabled: true
  boot:
    admin:
      client:
        url: http://localhost:8086